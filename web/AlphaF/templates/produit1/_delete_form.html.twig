<form method="post" action="{{ path('produit1_delete', {'id': produit1.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit1.id) }}">
    <button class="btn">Delete</button>
</form>
