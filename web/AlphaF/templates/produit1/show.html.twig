{% extends 'base.html.twig' %}

{% block title %}Produit1{% endblock %}

{% block body %}
    <h1>Produit1</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit1.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ produit1.nom }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ produit1.categorie }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ produit1.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('produit1_index') }}">back to list</a>

    <a href="{{ path('produit1_edit', {'id': produit1.id}) }}">edit</a>

    {{ include('produit1/_delete_form.html.twig') }}
{% endblock %}
