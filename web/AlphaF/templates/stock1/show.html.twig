{% extends 'base.html.twig' %}

{% block title %}Stock1{% endblock %}

{% block body %}
    <h1>Stock1</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stock1.id }}</td>
            </tr>
            <tr>
                <th>Dateentree</th>
                <td>{{ stock1.dateentree ? stock1.dateentree|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ stock1.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('stock1_index') }}">back to list</a>

    <a href="{{ path('stock1_edit', {'id': stock1.id}) }}">edit</a>

    {{ include('stock1/_delete_form.html.twig') }}
{% endblock %}
