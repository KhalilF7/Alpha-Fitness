{% extends 'base.html.twig' %}

{% block title %}<title>Alpha Fitness | Modifier Evènement</title>{% endblock %}
{% block innerblock %}
<div class="inner-block">
    <!-- Main content -->
    <section class="content">
        <div class="row">
            <div class="col-xs-12">
                <div class="box">
<div class="container">
    <a href="{{ path('list_event') }}"><i class="fa fa-arrow-circle-o-left">Retour</i></a>

    <div class="form-group">
    {{ form_start(form,{attr:{'id':'form'}}) }}

    <div class="card-footer">
        {{ form_end(form) }}
        <button type="submit" href="{{ path('add_event') }}" class="btn btn-primary">Modifier</button>
    </div>
</div>
</div>
                </div>
            </div>
        </div>
    </section>
</div>
{% endblock %}
