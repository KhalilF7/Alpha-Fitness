{% extends 'base.html.twig' %}

{% block title %}<title>Alpha Fitness | Modifier Catégorie Evènement</title>{% endblock %}
{% block innerblock %}
<div class="inner-block">
    <!-- Main content -->
    <section class="content">
        <div class="row">
            <div class="col-xs-12">
                <div class="box">
                    <a href="{{ path('list_categorie') }}"><i class="fa fa-arrow-circle-o-left">Retour</i></a>

                    {{ form_start(form,{attr:{'id':'form'}}) }}
    <div class="card-footer">
        {{ form_end(form) }}

        <button type="submit" href="{{ path('add_categorie') }}" class="btn btn-primary">modifier</button>
    </div>
                </div>
            </div>
        </div>
    </section>
</div>

{% endblock %}
