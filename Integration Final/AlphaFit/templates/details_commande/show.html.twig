{% extends 'base.html.twig' %}

{% block body %}
    <h1>DetailsCommande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ details_commande.id }}</td>
            </tr>
            <tr>
                <th>Numc</th>
                <td>{{ details_commande.numc }}</td>
            </tr>
            <tr>
                <th>Qteproduit</th>
                <td>{{ details_commande.qteproduit }}</td>
            </tr>
            <tr>
                <th>Prixproduit</th>
                <td>{{ details_commande.idproduit.idproduit.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('details_commande_index') }}">back to list</a>

    <a href="{{ path('details_commande_edit', {'id': details_commande.id}) }}">edit</a>

    {{ include('details_commande/_delete_form.html.twig') }}
{% endblock %}
