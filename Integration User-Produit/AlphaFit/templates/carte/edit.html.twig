{% extends 'base.html.twig' %}

{% block title %}<title>Alpha Fitness | Modifier Carte</title>{% endblock %}

{% block innerblock %}
    <div class="inner-block">
        <!-- Main content -->
        <section class="content">
            <div class="row">
                <div class="col-xs-12">
                    <div class="box">
                        <div class="box-header">
                            <a href="{{ path('carte_index') }}">Retour</a>
                            <h1>Edit Carte</h1>

                            {{ include('carte/_form.html.twig', {'button_label': 'Modifier'}) }}


                            {{ include('carte/_delete_form.html.twig') }}
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </div>
{% endblock %}
