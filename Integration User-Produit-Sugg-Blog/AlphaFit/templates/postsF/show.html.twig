{% extends 'basef.html.twig' %}

{% block title %}
    <title>Alpha Fitness | Posts</title>
{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" type="text/css" href="{{ asset('bundles/starrating/css/rating.css') }}" />
{% endblock %}
{#
{% block Blogs %}
    <div class="container py-lg-3">
    <h1>Posts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idp</th>
                <td>{{ post.idp }}</td>
            </tr>
            <tr>
                <th>Contenup</th>
                <td>{{ post.contenup }}</td>
            </tr>
            <tr>
                <th>Nbcmt</th>
                <td>{{ post.nbcmt }}</td>
            </tr>
            <tr>
                <th>Views</th>
                <td>{{ post.views }}</td>
            </tr>
            <tr>
                <th>Likes</th>
                <td>{{ post.likes }}</td>
            </tr>
            <tr>
                <th>Report</th>
                <td>{{ post.report }}</td>
            </tr>
            <tr>
                <th>DateP</th>
                <td>{{ post.dateP ? post.dateP|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Iduser</th>
                <td>{{ post.iduser }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('posts_index') }}">back to list</a>

    <a href="{{ path('posts_edit', {'idp': post.idp}) }}">edit</a>

    {{ include('posts/_delete_form.html.twig') }}
</div>
{% endblock %}

#}

 {% block Blogs %}
     <div class="row pt-md-3">
     <div class="col-lg-9 col-md-10 mx-auto">
     <div class="accordion" id="accordionExample">
     <div class="subject-card mt-4">
         <div class="subject-card-header p-0" id="headingOne">
             <a href="#" class="card__title p-3 p-lg-4 collapsed d-block" data-toggle="collapse" data-target="#collapseOne" aria-expanded="false"
                aria-controls="collapseOne">
                 <div class="row">
                     <div class="col-md-5 subject-img">
                         <img src="{{ asset(post.imgpost) }}"style="height:7cm;width:7cm;" alt="">
                     </div>
                     <div class="col-md-7 subject-content mt-md-0 mt-4">
                         <h3 style="color: #D6A7A7">{{ post.idPoster.username }} </h3>
                         <td style="color: #D6A7A7">{{ post.idPoster.email }} </td>
                         <p><a  {{post.id}} >{{ post.contenup }}</a>

                                                    <span class="course-number">
                                                        <span class="fa fa-comments-o" aria-hidden="true"> {{ post.nbcmt }}</span>
                                                        <span class="fa fa-eye" aria-hidden="true"> {{ post.views }}</span>
                                                        <span class="fa fa-star" aria-hidden="true"> {{ p.avis }}</span>
                                                    </span>

                                                <span class="date"aria-hidden="true">
								<label  rel="bookmark" class="entry-date" style="color: var(--title-color);">
									{{ post.dateP | date }}
								</label>
							</span>
                {% if post.idPoster.username == app.user.username and post.idPoster.email== app.user.email %}
                <br>
                <br>
                         <td>
                             <a class="btnSingel4"  href="{{ path('posts_edit', {'id': post.id}) }}">Modifier</a>

                         </td>
                         <td>
                             {#   <a href="{{ path('posts_show', {'idp': post.idp}) }}">show</a>#}
                             {#    <span class="label label-danger"></span>#}
                             <a  class="btnSingel4"  href="{{ path('posts_delete1',{'id':post.id}) }}">Supprimer</a>
                         </td>
                         {% else %}
                         <td>

                         </td>
                {% endif %}

                     </div>
                 </div>
               {#    <h4>AVIS</h4>
                 <p>{{ avis }}</p>#}
                 <br>
                 
                 {{ form_start(form,{'attr':{'id':'ratingFormB','class':'form-inline'}}) }}
                 {% if form.rating.vars.value >0  %}
                     <div class="input-group">
                         <td>Mon rate</td>
                         {{ form_widget(form.rating ,{'attr' : {'id':'ratingInput','disabled':'' }} )}}
                     </div>
                 {% else %}
                     <h4></h4>
                     <div class="input-group">
                         {{ form_widget(form.rating ,{'attr' : {'id':'ratingInput'}} )}}
                         <div class="input-group-append">
                             <input  class="btnSingel3" type="submit"/>
                         </div>
                     </div>

                 {% endif %}

                 {{ form_end(form) }}

                 <span class="collapsed">
                 Show More <i class="fa fa-chevron-down" aria-hidden="true"></i>
                </span>
                 {#  <span class="expanded">
                  "Show Less" <a class="fa fa-chevron-up" aria-hidden="true"></a>
                </span>#}
             </a>

         </div>

         <div id="collapseOne" class="collapse" aria-labelledby="headingOne" data-parent="#accordionExample" style="">

             <div> <form method="post" action="{{path('comments_new', {'idp': post.id}) }}">

                  <table>
                      <tr>
                          <td>Taper un commentaire</td>
                          <td> <input type="text" name="contenu" id="contenu">
                          </td>

                      </tr>
                      <br>
                      <tr>

                          <td>
                              <input class="btnSingel2" type="submit" value="Ajouter">
                          </td>
                      </tr>
                  </table>
               </form>
              </div>
             <br>
             <br>
             <h1 style="color: #ca2429">Commentaires</h1>
             <br>
             <div class="col-md-7 subject-content mt-md-0 mt-4">
                 {% for c in comments %}

                 <p> {{ c.contenuc }}</p>
                     <span class="course-number"><span class="fa fa-calendar-times-o" aria-hidden="false"></span>
                 <span class="date"aria-hidden="true">
								<label  rel="bookmark" class="entry-date">
									{{ c.datec | date }}
								</label>
                                <label>
                                    {{ c.idCommenter.username }}
                                </label>
							</span>
                 {% endfor %}
                         </span>
             </div>

         </div>

     </div>
 {% endblock %}
         {% block javascript %}
         <script src="{{ asset('bundles/starrating/js/rating.js') }}"></script>

{% endblock %}