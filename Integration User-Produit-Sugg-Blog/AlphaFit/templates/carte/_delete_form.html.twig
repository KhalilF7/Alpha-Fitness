<form method="post" action="{{ path('carte_delete', {'idcard': carte.idcard}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carte.idcard) }}">
    <button class="btn">Supprimer</button>
</form>
